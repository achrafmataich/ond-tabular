import { DragPreviewCardDirective } from './drag-preview-card.directive';

describe('DragPreviewCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DragPreviewCardDirective();
    expect(directive).toBeTruthy();
  });
});
